# Reference DeviceClass for manual testing - not used by automation scripts.
apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: gpu-test.nvidia.com
spec:
  selectors:
  - cel:
      expression: device.driver == 'gpu.nvidia.com'
